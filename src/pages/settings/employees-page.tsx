import { EmployeesTable } from '@/widgets/employees-table'

/** The employee roster — the people whose conversations get scored. */
export function EmployeesSettingsPage() {
  return <EmployeesTable />
}

export default EmployeesSettingsPage
